module.exports = intersects;

function intersects(a, b) {
    var ax = a.x;
    var ay = a.y;
    var bx = b.x;
    var by = b.y;
    // if (ax === bx && ay === by) {
    //     return false;
    // } else if (ax >= bx || ax <= bx) {

    // }
    // var xIntersects = ax>=
}